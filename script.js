var typed = new Typed(".typed", {
    strings: ["Forntend Developer" ,"Developer" ],
    typeSpeed: 60,
    startDelay: 900,
    showCursor: false,
    loop: true,
    backDelay: 900,
    backSpeed: 40,
  });

  let